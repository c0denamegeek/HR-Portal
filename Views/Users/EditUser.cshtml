@model HR_Portal.ViewModel.AdminViewModels.UserManagementViewModel
@{
    ViewData["Title"] = "Edit User";
}

<div class="row justify-content-center">
    <div class="col-lg-7">
        <div class="card">
            <div class="card-header">
                <i class="bi bi-person-gear me-2"></i>Edit — @Model.Name @Model.Surname
            </div>
            <div class="card-body">
                <partial name="_UserForm" model="Model" />
            </div>
        </div>
    </div>
</div>
